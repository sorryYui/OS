1.用户线程的调度与切换。
  用户线程的切换在应用程序内进行，通常采用非抢占式和更简单的规则。如时间片轮换规则，也无需用户模式和内核模式的求换。
  一个多用户线程的应用程序不能充分利用多CPU技术，内核一次只把一个CPU分给一个进程。因此一个进程中只有一个用户线程可以执行。
2.内核线程的调度与切换
  内核线程由内核来维护其上下文信息，调度是由内核以线程为单位进行的。内核线程的调度和切换都需要用户模式和内核模式之间的切换。
  调度可以为一个内核进程的多个线程分配多个CPU，使多个内核线程达到并行。
