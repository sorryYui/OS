创建新的进程包括为进程分配必要的资源，建立OS用于管理进程的数据结构等操作。
进程被创建时，除了得到OS各种资源外，他还能从父进程那里得到所需要的输入数据。
当新进程被创建的时，有两种执行的可能：
  1.父进程与子进程并发进行。
  2.父进程等待，直到某个或者全部子进程执行完毕。
新进程的地址空间也有两种可能：
  1.子进程共享父进程的空间地址。
  2.子进程拥有独立的空间地址。
调用创建新进程的系统调用来创建新进程的一般步骤如下：
  1.申请空白PCB。
  2.为新进程分配资源。
  3.初始化PCB。
  4.将新进程插入就绪队列。
