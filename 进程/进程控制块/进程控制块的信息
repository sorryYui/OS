一：进程标识符。进程标识符用于唯一标识一个进程。进程控制块中除了存在本进程的标识符外，还有其父进程，子进程的标识符。
二：处理机状态信息。
  1.通用寄存器信息。用户程序可以访问的寄存器，用于暂存信息。
  2.指令计数器。其中存放了要访问的下一条指令的地址。
  3.程序状态字PSW。其中包含状态信息，如条件码，执行方式等。
  4.用户栈指针。每一个用户进程都有一个与之相关的系统栈，用于存放过程和系统调用参数及调用地址，栈指针指向栈顶。
三：进程调度信息。进程调度信息包括进程状态信息，进程优先度等信息。
四：进程控制信息。进程控制信息包含程序和数据的地址，进程同步和通讯机制，资源清单和链接指针。

PS：用户栈：是用户进程空间中的一块区域，用于保存用户进程的子程序间相互调用的参数、返回值、返回点以及子程序(函数)的局部变量。
