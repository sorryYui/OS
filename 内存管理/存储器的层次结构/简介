存储器系统是一个具有不同容量，成本和访问时间的存储设备的层次结构。
在这个层次系统中，从高层到低层(L0-L5)，
较低层的存储设备更慢，容量更大，价格也较便宜；
最高层(L0)是少量快速CPU寄存器，CPU可以在一个时钟周期内访问它们；
接下来是一个或多个小型或中型基于SRAM的快速缓存器，可以在几个时钟周期内访问它们(L1 - L2)；
之后是一个大的基于DRAM的主存(L3)，可以在几十到几百的时钟周期内访问它们。
L4是慢速但是容量很大的本地磁盘。
L5是外部储存。
