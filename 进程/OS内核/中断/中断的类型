中断分为同步中断(也称内部中断或异常)和异步中断(也称外部异常)。
1.同步中断
  同步中断是当指令执行时由CPU控制单元产生的，之所以称为同步，是因为只有在一条指令终止执行后CPU才会发出中断。
2.异步中断
  异步中断是由其他硬件设备随机产生的。
  异步中断可以分成外部可屏蔽中断和外部不可屏蔽中断。
    1.外部可屏蔽中断。
      外部可屏蔽中断是有I/O设备产生的中断。
    2.外部不可屏蔽中断
      外部不可中断是由紧急事件引起的中断。如硬件的故障。
 
